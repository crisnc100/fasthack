# Backend Configuration (if using custom backend)
EXPO_PUBLIC_RORK_API_BASE_URL=http://localhost:3000

# Note: Google OAuth is now handled with mock authentication
# No external configuration needed for demo purposes